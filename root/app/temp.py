#!/usr/bin/env python3
import producto.controller
from producto.controller import guardar
from bar import foo

alpha = [2, 4, 6, 8, 10]
beta = ('hermanos', 'hermanas', 'primos', 'primas')
if len(alpha) == 3 and len(beta) == 2:
    print("SI-YES-BAI ")
else:
    print("NO-EZ")

